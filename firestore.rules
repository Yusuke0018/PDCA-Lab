rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 開発中は全てのアクセスを許可（本番環境では必ず変更してください）
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 12, 31);
    }
  }
}